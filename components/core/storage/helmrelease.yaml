apiVersion: v1
kind: Namespace
metadata:
  name: homystack-local-path

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: local-path
  namespace: homystack-local-path
spec:
  chart:
    spec:
      chart: local-path-provisioner
      version: "0.0.33"
      sourceRef:
        kind: HelmRepository
        name: homystack-local-path
        namespace: flux-system
  interval: 10m
  install:
    createNamespace: true